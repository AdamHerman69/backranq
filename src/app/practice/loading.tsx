import { PageSkeleton } from '@/components/ui/loading-patterns';

export default function PracticeLoading() {
    return (
        <PageSkeleton
            variant="workspace"
            label="Preparing your practice position"
        />
    );
}
