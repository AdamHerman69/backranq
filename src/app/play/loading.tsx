import { PageSkeleton } from '@/components/ui/loading-patterns';

export default function PlayLoading() {
    return (
        <PageSkeleton
            variant="workspace"
            label="Preparing Coach"
        />
    );
}
